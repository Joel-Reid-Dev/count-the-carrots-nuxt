{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "tsc-watch": "vue-tsc --noEmit --watch"
  },
  "dependencies": {
    "nuxt": "^3.12.4",
    "vue": "latest",
    "vue-tsc": "^2.0.26"
  },
  "devDependencies": {
    "sass": "^1.77.8"
  }
}
